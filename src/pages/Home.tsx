import React from "react";
import HeroCarousel from "../components/HeroCarousel";

const Home: React.FC = () => {
  return (
    <div>
      <HeroCarousel />
      {/* You could add other home page sections here if desired */}
    </div>
  );
};

export default Home;
